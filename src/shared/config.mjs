// WOBBLE — единый конфиг математики и экономики (тюнился в песочнице, docs/13 §1/§4)
export const CONFIG = {
  // --- математика крена (px в координатах канона; клиент масштабирует) ---
  oMax: 30,          // потолок одного промаха: |o| ≤ oMax  (анти-гриф: oMax < limitMin)
  limit0: 96,        // Предел |L| на пустой башне
  limitMin: 42,      // Предел на большой высоте (инвариант: > oMax)
  limitRefH: 80,     // высота (этажей), к которой Предел полностью сжат
  blockW: 90,        // ширина этажа (канон-координаты)
  blockH: 30,

  // --- скорость свипа (клиент; сервер не проверяет тайминг, только offset) ---
  sweepSpeed: 1.7,   // рад/с базовая
  sweepRamp: 0.03,   // прирост скорости за этаж — ГЛАВНЫЙ рычаг каденции:
                     // бот-сим (test/math): 0.03 → медиана обвала 92 эт. (p10=66, p90=134), цель 60–120

  // --- экономика (docs/13 §4, решение 09.07) ---
  energyCap: 3,
  energyRegenMs: 3 * 60 * 60 * 1000, // 1 блок / 3 часа
  energyStart: 3,                    // новичок/судья играет сразу

  // --- turn-lock (docs/14 §2) ---
  lockTtlMs: 15_000,
  heartbeatMs: 5_000,

  // --- «не подряд» (фидбек юзера 14.07): повторный дроп того же игрока —
  // после чужого дропа ИЛИ через кулдаун. Дешёвый соло-гриф x2-x3 кирпича умирает,
  // судья в мёртвый час не заперт навсегда. Обвал сбрасывает (новая башня — чистый лист).
  // 90с → 60с (15.07): ритм судьи-одиночки - дроп → минута практики → дроп.
  repeatCooldownMs: 60_000,

  // --- practice (обвал гарантирован ~30 с: docs/14 §3.4) ---
  practice: { limit0: 60, limitMin: 18, limitRefH: 14 },
};

// Инвариант анти-грифа: один блок с ровной башни не роняет НИКОГДА.
if (CONFIG.oMax >= CONFIG.limitMin) {
  throw new Error('CONFIG invariant broken: oMax must be < limitMin (anti-grief)');
}
